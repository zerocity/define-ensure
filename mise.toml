[tools]
deno = "latest"
"npm:@mariozechner/pi-coding-agent" = "latest"

[env]
EDITOR = "code"

[tasks]
test = "deno test"
check = "deno check src/mod.ts"
lint = "deno lint"
fmt = "deno fmt"
dev = "deno run --watch main.ts"
ci = ["deno check src/mod.ts", "deno lint", "deno test"]
